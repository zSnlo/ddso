## 文件路径

```
├──  assets
├──  components
│    └──  layoutbase      // 页面布局组件模块
│         ├──  BreadCrumb.vue  // 面包屑导航
│         ├──  Head.vue        // 头部菜单
│         ├──  LoginForm.vue   // 登录窗口
│         ├──  Side.vue        // 侧边栏导航
│         └──  ...             // 更多
│    └──  pop       // 弹窗组件模块
│         ├──  AddPoint.vue    // 添加点位弹窗
|         ...                  // 更多
│         └──  Side.vue        // 侧边栏导航
│    └──  safe      // 安全模块
│         └──  goal        // 目标责任
│         └──  inspect     // 智能巡检
│              ├──  ...
│              └──  ...
│         ├──  rules       // 制度管理
│         ├──  edu_train   // 教育培训
│         ├──  scene       // 现场管理
│         ├──  risk_danger // 风险隐患管理
│         ├──  urgent      // 应急管理
│         ├──  accident    // 事故管理
│         └──  improve     // 持续改进
│    └──  health    // 健康模块
│         └──
│    └──  envir     // 环境模块
│         └──
├──  filter
├──  router
└──  utils
```